# LOCK_FAILED parameters

[object_name]
description = name of the lock
flags = required

[key]
description = the key involved with the failure
flags = required

[error]
description = reason for the failure as one or two words (i.e. "timedout")
flags = required

# vim: syntax=dosini
